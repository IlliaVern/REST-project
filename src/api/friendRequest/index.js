// import {Router} from 'express'
// import {middleware as query} from 'querymen'
// import {middleware as body} from 'bodymen'
// import {token} from '../../services/passport'
// import {accept, decline } from './controller'
import { schema } from './model'
export FriendRequest, { schema } from './model'

// const router = new Router()
// const { requester, recipient, status } = schema.tree

// router.get('/', token({required: true}), show)

// router.post('/', token({required: true}), create)

// router.put('/:friendRequestId', token({required: true}), )

// export default router
